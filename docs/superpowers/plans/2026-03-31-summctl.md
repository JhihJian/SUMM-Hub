# summctl 实现计划

## 概述

实现一个 Rust CLI 工具，统一管理 SUMM-Hub 的所有 consumer。

**Spec 文档:** `docs/superpowers/specs/2026-03-31-summctl-design.md`

## 文件结构

```
summctl/
├── Cargo.toml              # 项目配置和依赖
├── src/
│   ├── main.rs            # 入口点，命令分发
│   ├── cli.rs             # clap 命令定义
│   ├── config.rs          # consumers.yaml 解析
│   ├── docker.rs          # docker compose 执行器
│   ├── status.rs          # 容器状态检测
│   ├── topology.rs        # Subject 拓扑生成
│   └── error.rs           # 错误类型定义
└── tests/
    └── integration.rs     # 集成测试

consumers.yaml              # 统一配置文件（项目根目录）
```

## 任务分解

---

### Task 1: 项目初始化

**目标:** 创建 Rust 项目骨架，配置依赖

**文件:**
- `summctl/Cargo.toml` - 新建
- `summctl/src/main.rs` - 新建

**步骤:**

```bash
# 1. 创建项目
cd /data/dev/SUMM-Hub
cargo new summctl --name summctl

# 2. 更新 Cargo.toml
```

**Cargo.toml 内容:**
```toml
[package]
name = "summctl"
version = "0.1.0"
edition = "2021"
description = "SUMM-Hub Consumer Management CLI"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
tokio = { version = "1", features = ["full"] }
thiserror = "1"
tracing = "0.1"
tracing-subscriber = "0.3"
regex = "1"

[dev-dependencies]
tempfile = "3"
```

**main.rs 骨架:**
```rust
mod cli;
mod config;
mod docker;
mod error;
mod status;
mod topology;

#[tokio::main]
async fn main() {
    tracing_subscriber::fmt::init();
    if let Err(e) = cli::run().await {
        eprintln!("Error: {}", e);
        std::process::exit(1);
    }
}
```

**验证:**
```bash
cd /data/dev/SUMM-Hub/summctl
cargo build
# 应该编译成功，输出 "Hello, world!"
```

---

### Task 2: 错误类型定义

**目标:** 定义统一的错误类型

**文件:**
- `summctl/src/error.rs` - 新建

**内容:**
```rust
use thiserror::Error;

#[derive(Debug, Error)]
pub enum SummctlError {
    #[error("Consumer not found: {0}")]
    NotFound(String),

    #[error("Docker compose command failed: {0}")]
    DockerError(String),

    #[error("Failed to parse config: {0}")]
    ConfigError(#[from] serde_yaml::Error),

    #[error("IO error: {0}")]
    IoError(#[from] std::io::Error),

    #[error("Environment variable not set: {0}")]
    EnvVarMissing(String),

    #[error("Config file not found: {0}")]
    ConfigNotFound(String),
}

pub type Result<T> = std::result::Result<T, SummctlError>;
```

**验证:**
```bash
cd /data/dev/SUMM-Hub/summctl
cargo check
# 应该通过类型检查
```

---

### Task 3: 配置解析

**目标:** 解析 consumers.yaml，支持环境变量替换

**文件:**
- `summctl/src/config.rs` - 新建
- `consumers.yaml` - 新建（项目根目录）

**config.rs 内容:**
```rust
use crate::error::{Result, SummctlError};
use serde::Deserialize;
use std::collections::HashMap;
use std::env;
use std::path::{Path, PathBuf};

/// consumers.yaml 根结构
#[derive(Debug, Deserialize, Clone)]
pub struct Config {
    #[serde(default)]
    pub defaults: Option<Defaults>,
    pub consumers: HashMap<String, ConsumerConfig>,
}

#[derive(Debug, Deserialize, Clone)]
pub struct Defaults {
    pub nats_url: Option<String>,
    pub log_level: Option<String>,
}

/// 单个 Consumer 配置
#[derive(Debug, Deserialize, Clone)]
pub struct ConsumerConfig {
    pub description: String,
    pub path: PathBuf,
    pub subjects: Subjects,
    #[serde(default)]
    pub env: HashMap<String, String>,
}

#[derive(Debug, Deserialize, Clone)]
pub struct Subjects {
    pub subscribe: Vec<String>,
    pub publish: Vec<String>,
}

impl Config {
    /// 从文件加载配置
    pub fn load(path: &Path) -> Result<Self> {
        let content = std::fs::read_to_string(path)
            .map_err(|_| SummctlError::ConfigNotFound(path.display().to_string()))?;
        let mut config: Config = serde_yaml::from_str(&content)?;

        // 替换环境变量
        for consumer in config.consumers.values_mut() {
            for value in consumer.env.values_mut() {
                *value = expand_env_vars(value)?;
            }
        }

        Ok(config)
    }

    /// 获取 consumer 配置
    pub fn get_consumer(&self, name: &str) -> Result<&ConsumerConfig> {
        self.consumers
            .get(name)
            .ok_or_else(|| SummctlError::NotFound(name.to_string()))
    }
}

/// 展开环境变量 ${VAR} 和 ${VAR:-default}
fn expand_env_vars(s: &str) -> Result<String> {
    let re = regex::Regex::new(r"\$\{([^}]+)\}").unwrap();
    let mut result = s.to_string();

    for cap in re.captures_iter(s) {
        let full = &cap[0];
        let inner = &cap[1];

        let (var_name, default) = if let Some(idx) = inner.find(":-") {
            (&inner[..idx], Some(&inner[idx + 2..]))
        } else {
            (inner, None)
        };

        let replacement = match (env::var(var_name).ok(), default) {
            (Some(val), _) => val,
            (None, Some(def)) => def.to_string(),
            (None, None) => return Err(SummctlError::EnvVarMissing(var_name.to_string())),
        };

        result = result.replace(full, &replacement);
    }

    Ok(result)
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_expand_env_vars_with_default() {
        env::remove_var("TEST_VAR");
        let result = expand_env_vars("${TEST_VAR:-default_value}").unwrap();
        assert_eq!(result, "default_value");
    }

    #[test]
    fn test_expand_env_vars_with_value() {
        env::set_var("TEST_VAR", "actual_value");
        let result = expand_env_vars("${TEST_VAR:-default}").unwrap();
        assert_eq!(result, "actual_value");
        env::remove_var("TEST_VAR");
    }
}
```

**consumers.yaml 内容:**
```yaml
# SUMM-Hub Consumer 配置清单
# 路径: /data/dev/SUMM-Hub/consumers.yaml

defaults:
  nats_url: nats://host.docker.internal:6422
  log_level: info

consumers:
  feishu-connector:
    description: "飞书双向消息连接器"
    path: ./consumer/feishu-connector
    subjects:
      subscribe: [summ.ai.input]
      publish: [summ.ai.output]
    env:
      FEISHU_APP_ID: ${FEISHU_APP_ID:-}
      FEISHU_APP_SECRET: ${FEISHU_APP_SECRET:-}
      NATS_URL: ${NATS_URL:-nats://host.docker.internal:6422}

  claude-code-consumer:
    description: "Claude Code AI处理"
    path: ./consumer/claude-code-consumer/deploy
    subjects:
      subscribe: [summ.ai.input]
      publish: [summ.ai.output, summ.ai.error]
    env:
      ANTHROPIC_BASE_URL: ${ANTHROPIC_BASE_URL:-}
      ANTHROPIC_AUTH_TOKEN: ${ANTHROPIC_AUTH_TOKEN:-}
      NATS_URL: nats://summ-hub-nats-1:4222

  script-consumer:
    description: "脚本执行消费器"
    path: ./consumer/script-consumer
    subjects:
      subscribe: [summ.notify.info]
      publish: []
    env:
      NATS_URL: ${NATS_URL:-nats://host.docker.internal:6422}
      DINGTALK_TOKEN: ${DINGTALK_TOKEN:-}
```

**验证:**
```bash
cd /data/dev/SUMM-Hub/summctl
cargo test config::tests
# 应该通过单元测试
```

---

### Task 4: Docker Compose 执行器

**目标:** 封装 docker compose 命令执行

**文件:**
- `summctl/src/docker.rs` - 新建

**内容:**
```rust
use crate::error::{Result, SummctlError};
use std::path::Path;
use std::process::Command;

pub struct DockerCompose {
    project_path: std::path::PathBuf,
}

impl DockerCompose {
    pub fn new(project_path: &Path) -> Self {
        Self {
            project_path: project_path.to_path_buf(),
        }
    }

    /// 启动容器
    pub async fn up(&self) -> Result<()> {
        self.run_command(&["up", "-d"]).await
    }

    /// 停止容器
    pub async fn down(&self) -> Result<()> {
        self.run_command(&["down"]).await
    }

    /// 重启容器
    pub async fn restart(&self) -> Result<()> {
        self.run_command(&["restart"]).await
    }

    /// 获取日志
    pub fn logs(&self, follow: bool) -> Result<()> {
        let mut args = vec!["logs"];
        if follow {
            args.push("-f");
        }
        self.run_command_sync(&args)
    }

    /// 检查服务状态，返回 "running" | "stopped" | "error"
    pub async fn status(&self) -> Result<String> {
        let output = Command::new("docker")
            .args(["compose", "ls", "--format", "json"])
            .current_dir(&self.project_path)
            .output()
            .map_err(|e| SummctlError::DockerError(e.to_string()))?;

        if !output.status.success() {
            return Ok("stopped".to_string());
        }

        let stdout = String::from_utf8_lossy(&output.stdout);
        // 简单检查是否有 running 状态
        if stdout.contains("running") {
            Ok("running".to_string())
        } else {
            Ok("stopped".to_string())
        }
    }

    async fn run_command(&self, args: &[&str]) -> Result<()> {
        let full_args = ["compose"].iter().chain(args).copied().collect::<Vec<_>>();

        let output = Command::new("docker")
            .args(&full_args)
            .current_dir(&self.project_path)
            .output()
            .map_err(|e| SummctlError::DockerError(e.to_string()))?;

        if !output.status.success() {
            let stderr = String::from_utf8_lossy(&output.stderr);
            return Err(SummctlError::DockerError(stderr.to_string()));
        }

        Ok(())
    }

    fn run_command_sync(&self, args: &[&str]) -> Result<()> {
        let full_args = ["compose"].iter().chain(args).copied().collect::<Vec<_>>();

        let status = Command::new("docker")
            .args(&full_args)
            .current_dir(&self.project_path)
            .status()
            .map_err(|e| SummctlError::DockerError(e.to_string()))?;

        if !status.success() {
            return Err(SummctlError::DockerError("Command failed".to_string()));
        }

        Ok(())
    }
}
```

**验证:**
```bash
cd /data/dev/SUMM-Hub/summctl
cargo check
```

---

### Task 5: 状态检测

**目标:** 检测所有 consumer 的运行状态

**文件:**
- `summctl/src/status.rs` - 新建

**内容:**
```rust
use crate::config::{Config, ConsumerConfig};
use crate::docker::DockerCompose;
use crate::error::Result;
use serde::Serialize;

#[derive(Debug, Serialize, Clone)]
pub struct ConsumerStatus {
    pub name: String,
    pub description: String,
    pub status: String,
    pub subjects_in: Vec<String>,
    pub subjects_out: Vec<String>,
}

pub struct StatusChecker<'a> {
    config: &'a Config,
}

impl<'a> StatusChecker<'a> {
    pub fn new(config: &'a Config) -> Self {
        Self { config }
    }

    /// 获取所有 consumer 状态
    pub async fn check_all(&self) -> Result<Vec<ConsumerStatus>> {
        let mut statuses = Vec::new();

        for (name, consumer_config) in &self.config.consumers {
            let status = self.check_one(name, consumer_config).await?;
            statuses.push(status);
        }

        // 按名称排序
        statuses.sort_by(|a, b| a.name.cmp(&b.name));
        Ok(statuses)
    }

    /// 获取单个 consumer 状态
    pub async fn check_one(
        &self,
        name: &str,
        config: &ConsumerConfig,
    ) -> Result<ConsumerStatus> {
        let compose = DockerCompose::new(&config.path);
        let status = compose.status().await.unwrap_or_else(|_| "unknown".to_string());

        Ok(ConsumerStatus {
            name: name.to_string(),
            description: config.description.clone(),
            status,
            subjects_in: config.subjects.subscribe.clone(),
            subjects_out: config.subjects.publish.clone(),
        })
    }

    /// 格式化为表格输出
    pub fn format_table(statuses: &[ConsumerStatus]) -> String {
        let mut result = String::new();
        result.push_str(&format!(
            "{:<20} {:<10} {:<25} {:<25}\n",
            "NAME", "STATUS", "SUBJECTS IN", "SUBJECTS OUT"
        ));
        result.push_str(&"-".repeat(80));
        result.push('\n');

        for s in statuses {
            let subjects_in = s.subjects_in.join(", ");
            let subjects_out = if s.subjects_out.is_empty() {
                "-".to_string()
            } else {
                s.subjects_out.join(", ")
            };
            result.push_str(&format!(
                "{:<20} {:<10} {:<25} {:<25}\n",
                s.name, s.status, subjects_in, subjects_out
            ));
        }

        result
    }
}
```

**验证:**
```bash
cd /data/dev/SUMM-Hub/summctl
cargo check
```

---

### Task 6: 拓扑生成

**目标:** 生成 Subject 拓扑图

**文件:**
- `summctl/src/topology.rs` - 新建

**内容:**
```rust
use crate::config::Config;
use crate::error::Result;
use std::collections::HashMap;

#[derive(Debug)]
pub struct Topology {
    /// subject -> [(consumer, direction)]
    pub edges: HashMap<String, Vec<(String, Direction)>>,
}

#[derive(Debug, Clone)]
pub enum Direction {
    Subscribe,
    Publish,
}

impl Topology {
    pub fn from_config(config: &Config) -> Self {
        let mut edges: HashMap<String, Vec<(String, Direction)>> = HashMap::new();

        for (name, consumer) in &config.consumers {
            for subject in &consumer.subjects.subscribe {
                edges
                    .entry(subject.clone())
                    .or_default()
                    .push((name.clone(), Direction::Subscribe));
            }
            for subject in &consumer.subjects.publish {
                edges
                    .entry(subject.clone())
                    .or_default()
                    .push((name.clone(), Direction::Publish));
            }
        }

        Self { edges }
    }

    /// 格式化为树形输出
    pub fn format_tree(&self) -> String {
        let mut result = String::new();
        result.push_str("NATS Subject Topology\n");
        result.push_str("=====================\n\n");

        let mut subjects: Vec<_> = self.edges.keys().collect();
        subjects.sort();

        for subject in subjects {
            result.push_str(&format!("{}\n", subject));
            let edges = &self.edges[subject];

            for (i, (consumer, direction)) in edges.iter().enumerate() {
                let prefix = if i == edges.len() - 1 { "└─►" } else { "├─►" };
                let arrow = match direction {
                    Direction::Subscribe => "SUB",
                    Direction::Publish => "PUB",
                };
                result.push_str(&format!("  {} {} ({})\n", prefix, consumer, arrow));
            }
            result.push('\n');
        }

        result
    }
}
```

**验证:**
```bash
cd /data/dev/SUMM-Hub/summctl
cargo check
```

---

### Task 7: CLI 命令定义

**目标:** 使用 clap 定义所有 CLI 命令

**文件:**
- `summctl/src/cli.rs` - 新建

**内容:**
```rust
use crate::config::Config;
use crate::docker::DockerCompose;
use crate::error::Result;
use crate::status::StatusChecker;
use crate::topology::Topology;
use clap::{Parser, Subcommand};
use std::path::Path;

#[derive(Parser)]
#[command(name = "summctl")]
#[command(about = "SUMM-Hub Consumer Management CLI", long_about = None)]
pub struct Cli {
    /// Path to consumers.yaml
    #[arg(short, long, global = true, default_value = "consumers.yaml")]
    config: String,

    #[command(subcommand)]
    command: Commands,
}

#[derive(Subcommand)]
enum Commands {
    /// List all consumers and their status
    Status,

    /// Start a consumer
    Start { name: String },

    /// Stop a consumer
    Stop { name: String },

    /// Restart a consumer
    Restart { name: String },

    /// Show detailed info about a consumer
    Info { name: String },

    /// Show consumer logs
    Logs {
        name: String,
        /// Follow log output
        #[arg(short, long)]
        follow: bool,
    },

    /// Show NATS subject topology
    Topology,
}

pub async fn run() -> Result<()> {
    let cli = Cli::parse();
    let config_path = Path::new(&cli.config);
    let config = Config::load(config_path)?;

    match cli.command {
        Commands::Status => {
            let checker = StatusChecker::new(&config);
            let statuses = checker.check_all().await?;
            println!("{}", StatusChecker::format_table(&statuses));
        }

        Commands::Start { name } => {
            let consumer = config.get_consumer(&name)?;
            let compose = DockerCompose::new(&consumer.path);
            compose.up().await?;
            println!("Started: {}", name);
        }

        Commands::Stop { name } => {
            let consumer = config.get_consumer(&name)?;
            let compose = DockerCompose::new(&consumer.path);
            compose.down().await?;
            println!("Stopped: {}", name);
        }

        Commands::Restart { name } => {
            let consumer = config.get_consumer(&name)?;
            let compose = DockerCompose::new(&consumer.path);
            compose.restart().await?;
            println!("Restarted: {}", name);
        }

        Commands::Info { name } => {
            let consumer = config.get_consumer(&name)?;
            let compose = DockerCompose::new(&consumer.path);
            let status = compose.status().await?;

            println!("Name:        {}", name);
            println!("Description: {}", consumer.description);
            println!("Status:      {}", status);
            println!("Path:        {}", consumer.path.display());
            println!();
            println!("Subjects:");
            println!("  Subscribe: {}", consumer.subjects.subscribe.join(", "));
            println!("  Publish:   {}", consumer.subjects.publish.join(", "));
        }

        Commands::Logs { name, follow } => {
            let consumer = config.get_consumer(&name)?;
            let compose = DockerCompose::new(&consumer.path);
            compose.logs(follow)?;
        }

        Commands::Topology => {
            let topology = Topology::from_config(&config);
            println!("{}", topology.format_tree());
        }
    }

    Ok(())
}
```

**验证:**
```bash
cd /data/dev/SUMM-Hub/summctl
cargo build --release
./target/release/summctl --help
# 应该显示帮助信息
```

---

### Task 8: 集成测试

**目标:** 编写集成测试验证核心功能

**文件:**
- `summctl/tests/integration.rs` - 新建

**内容:**
```rust
use std::io::Write;
use tempfile::NamedTempFile;

fn create_test_config() -> NamedTempFile {
    let mut file = NamedTempFile::new().unwrap();
    writeln!(
        file,
        r#"
defaults:
  nats_url: nats://localhost:4222

consumers:
  test-consumer:
    description: "Test consumer"
    path: ./test
    subjects:
      subscribe: [test.input]
      publish: [test.output]
    env:
      TEST_VAR: ${TEST_VAR:-default}
"#
    )
    .unwrap();
    file
}

#[test]
fn test_config_load() {
    let file = create_test_config();
    let config = summctl::config::Config::load(std::path::Path::new(file.path())).unwrap();

    assert!(config.consumers.contains_key("test-consumer"));
    let consumer = &config.consumers["test-consumer"];
    assert_eq!(consumer.description, "Test consumer");
    assert_eq!(consumer.subjects.subscribe, vec!["test.input"]);
    assert_eq!(consumer.subjects.publish, vec!["test.output"]);
}

#[test]
fn test_topology_generation() {
    let file = create_test_config();
    let config = summctl::config::Config::load(std::path::Path::new(file.path())).unwrap();
    let topology = summctl::topology::Topology::from_config(&config);

    assert!(topology.edges.contains_key("test.input"));
    assert!(topology.edges.contains_key("test.output"));
}

#[test]
fn test_env_var_expansion() {
    std::env::set_var("TEST_VAR", "actual_value");
    let file = create_test_config();
    let config = summctl::config::Config::load(std::path::Path::new(file.path())).unwrap();

    let consumer = &config.consumers["test-consumer"];
    assert_eq!(consumer.env.get("TEST_VAR").unwrap(), "actual_value");

    std::env::remove_var("TEST_VAR");
}
```

**更新 lib.rs:**
```rust
// summctl/src/lib.rs
pub mod cli;
pub mod config;
pub mod docker;
pub mod error;
pub mod status;
pub mod topology;

pub use error::{Result, SummctlError};
```

**验证:**
```bash
cd /data/dev/SUMM-Hub/summctl
cargo test
# 所有测试应该通过
```

---

### Task 9: 构建发布版本

**目标:** 构建可分发的二进制文件

**步骤:**
```bash
cd /data/dev/SUMM-Hub/summctl

# 构建发布版本
cargo build --release

# 验证二进制
./target/release/summctl --version
./target/release/summctl --help
```

**验证命令:**
```bash
# 在项目根目录运行
cd /data/dev/SUMM-Hub
./summctl/target/release/summctl -c consumers.yaml status
# 应该显示所有 consumer 状态
```

---

### Task 10: 文档和使用说明

**目标:** 添加使用文档

**文件:**
- `docs/summctl.md` - 新建

**内容:**
```markdown
# summctl 使用指南

## 安装

```bash
cd /data/dev/SUMM-Hub/summctl
cargo build --release
sudo cp target/release/summctl /usr/local/bin/
```

## 配置

配置文件位于项目根目录 `consumers.yaml`。

## 命令

### 查看状态
```bash
summctl status
```

### 启动/停止/重启
```bash
summctl start feishu-connector
summctl stop feishu-connector
summctl restart feishu-connector
```

### 查看详情
```bash
summctl info feishu-connector
```

### 查看日志
```bash
summctl logs feishu-connector -f
```

### 查看拓扑
```bash
summctl topology
```

## 添加新 Consumer

1. 在 `consumers.yaml` 中添加配置
2. 确保 `path` 指向包含 `docker-compose.yml` 的目录
3. 声明 `subjects.subscribe` 和 `subjects.publish`

## 环境变量

配置支持 `${VAR}` 和 `${VAR:-default}` 语法。
敏感信息应通过环境变量传入，不要硬编码。
```

---

## 验证清单

- [ ] `cargo build --release` 成功
- [ ] `cargo test` 所有测试通过
- [ ] `summctl status` 显示所有 consumer
- [ ] `summctl topology` 显示 Subject 拓扑
- [ ] `summctl start/stop <name>` 正常工作
- [ ] 文档完整

## 实现顺序

```
Task 1 ─► Task 2 ─► Task 3 ─► Task 4 ─► Task 5
                                           │
                                           ▼
         Task 10 ◄── Task 9 ◄── Task 8 ◄── Task 7 ◄── Task 6
```

## 预计工作量

| Task | 预计时间 |
|------|----------|
| Task 1-2 | 10 分钟 |
| Task 3-4 | 20 分钟 |
| Task 5-6 | 15 分钟 |
| Task 7 | 15 分钟 |
| Task 8 | 15 分钟 |
| Task 9-10 | 10 分钟 |
| **总计** | **~1.5 小时** |
